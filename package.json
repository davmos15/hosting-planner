{
  "name": "hosting-planner",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "python3 -m http.server 8000",
    "dev": "python3 -m http.server 8000",
    "build": "node build.js",
    "vercel-build": "node build.js"
  },
  "dependencies": {
    "@vercel/edge": "^0.1.2"
  }
}
